# AGENTS.md

## Agent skills

### Issue tracker

GitHub Issues at `github.com/abuabdillahi/abuabdillahi.com`. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical 5-label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at repo root (neither exists yet — they'll be authored lazily by `/grill-with-docs` when terms or decisions need recording). See `docs/agents/domain.md`.
